---
import { langSelector } from '@/utils/lang-selector'
import { labels } from '@/i18n/home/config'

import BaseLayout from '@/layouts/BaseLayout.astro'

import About from '@comp/sections/About.astro'
import Education from '@comp/sections/Education.astro'
import Experience from '@comp/sections/Experience.astro'
import Projects from '@comp/sections/Projects.astro'
import Skills from '@comp/sections/Skills.astro'

const { currentLocale = 'es' } = Astro
const { SEO, ABT, EXP, PROJ, EDU, SKL } = langSelector({ currentLocale, labels })
---

<BaseLayout title=`${SEO.TITLE_1} · ${SEO.TITLE_2}` description={SEO.DESC}>
	<About LANG={ABT} />
	<Experience LANG={EXP} />
	<Projects LANG={PROJ} />
	<Education LANG={EDU} />
	<Skills LANG={SKL} />
</BaseLayout>
